# Step 5: clarify optional Courier signup

Replace the entire existing ALTMojaveExpressBoardScript source with ALTMojaveExpressBoardScript.txt. Retain its existing Object script type and record. Compile it, then save NVO.esm.

The revised flier says that accepting immediately begins the Courier story and that leaving keeps signup available. It replaces the donor's instruction to visit an office in the Hub, which did not describe what the button actually does. The donor's fade, ALTGO change, Doc Mitchell preparation, and delayed ALTStartUDF call are preserved. The callback rechecks ALTGO before starting the transition.

The user reported successful compilation. The departure stages in ALTStartQscript remain as supplied by the donor. No ESM or installed game file was modified by this source preparation. The assistant has not compiled or tested this change. The user handles GECK and testing.
