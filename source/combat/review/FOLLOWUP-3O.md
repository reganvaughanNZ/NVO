# Packet 3O: bounded implementation follow-up

The3N1 Baron checkpoint passed on the user's new game and one reload: background53,5108caps,120HP before/after each direct retired-callback call, zero matching OnHit handler-priority groups. xNVSE precompiled the helper successfully; no relevant compile/runtime errors were found. Native320 recorded new-game session1, successful reload session2, and no negative actor-value applications. This establishes the targeted current new/load retirement check; it is not a migration test for every pre-retirement save. An unattributed co-save no-handler warning was preserved in the review instead of claiming all extender logs are warning-free. Source/bytecode/reference retirement remains supported by3N saved-record verification. Temporary manual check files were archived; logs and protected game assets remain unchanged.

Added original offline AdmissionLedger sources beside the3N pure model, outside the production CMake target. Fixed retained capacity, per-target application key, explicit incarnations/revisions, session/generation/cookie tokens, duplicate/conflict handling, one simulated start/acknowledgment, failure retention, and verified monotonic retirement. No engine/logging/apply API. Source review specifically checked no active eviction, no log-budget dependency, preflight before cleanup, no cookie/generation wrap, no game pointers retained, and no mutable state access from rejected foreign threads.47 x86 MSVC checks passed with warnings treated as errors. No production DLL rebuilt or installed.

Limits are contractual, not hidden: envelope verification/revisions and ordered producer closure are supplied by fixtures, not yet by verified live providers. Allocation failure/counter exhaustion were inspected but not induced. Reset invalidates this bookkeeping only; it does not replace the native wrapper's outstanding continuation rules. This is an offline implementation of part of317-08, not closure of317-01/05/06/07/08. Original Ultra317 remains frozen and the overall pre-damage gate remains HOLD.

Next proposed packet: address the actual flight-tracking reservation before private-projectile substitution (317-07), with bounded capacity independent of diagnostic limits and an explicit post-selection failure policy. Review supplied native/provider source first; add a runtime test only for the changed path. Do not repeat the complete historic stress/miss suite or enable damage. Ask before beginning this next packet.

User reported ~55seconds after the intro until the background menu. Read-only inspection found no55-second timer in ALTStartQ; its8-second timer occurs after creation. Recorded the stage chain and candidate transition points under source/startup-delay. Actual cause remains unmeasured. Keep a startup timing/fix packet separate from combat work, as this was a note rather than a replacement objective.
