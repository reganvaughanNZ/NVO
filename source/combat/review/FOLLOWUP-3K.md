# Packet 3K — limited follow-up to Ultra317

Native319 introduces net actor-value observations around the existing ITR DamageActorValue chain. It does not enable damage ownership or resolve finding317-01 by itself. The directly called getter/remapper interfaces were inspected in a read-only capture at the main menu, then bounded guards were derived. Existing downstream engine hooks are preserved, not exhaustively fingerprinted.

Fourteen offline checks passed with mocked engine reads. Actual new entry/return bridge instructions match the probe; existing flight, copy and hit-transaction bridges are unchanged. Scopes require receiver/source agreement and stable session. Nested intervals are explicitly inclusive, invalid identities/read failures suppress interpretation, and depth/log limits preserve original execution. These checks do not substitute for live actor-value validation.

Installed319 initialized its three table observers successfully at the main menu. No game/save was loaded. The player's three-hit/reload checkpoint is pending. Before/after getter differences describe the net change across an entire provider call; they cannot establish each internal write or the final input supplied to every engine helper. The earlier entry52 / pre-health-104 discrepancy is still unexplained pending this evidence.

The overall pre-damage gate remains HOLD. Continue from the existing review after the user's test; do not repeat the full Ultra audit. Mass/units/contact/admission rules and the BrahminBaron extra DamageAV/Kill owner remain outstanding before damage activation.

No ESM, configuration, health balance or flight changes. The RD-free ESM was pinned during installation. The healthy kit console hint was removed; failure diagnostics and manual kits remain.

## First live capture

See `../step3k/captures/playtest-ef0816806766/REVIEW.md`. The observer supplied valid HP/condition net changes and demonstrated condition clamping. All48 detailed HP losses were twice the earlier hit input; the cause remains unproven. One new-game session only, no reload. Detail budget filled with player-to-other 9mm hits. Gate remains HOLD; a compact reload/rifle/punch check is still required, without repeating the full test.

## Reload checkpoint completed

Native319 capture1930643b54d3 contains two successful save loads, rifle65->6HP before reload and punch65->64.0999985HP after reload, with both associated condition changes valid. The requested player-to-NPC net-value/reload checkpoint passes. Universal path coverage and the cause of factor2 remain unproven; overall pre-damage HOLD unchanged. See step3k/CHECKPOINT-RESULT.json. Native320/3K1 then adds only a once-per-process console banner; combat and flight source bytes remain identical to319.
