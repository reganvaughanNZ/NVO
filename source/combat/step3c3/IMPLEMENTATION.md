# Packet 3C3: locate the missing vertical movement (308)

Purpose: locate why the first 3C2 gravity-adjusted segment loses almost exactly the contribution of local Z. This is a diagnostic packet, not an accepted gravity fix. Retains the existing two private combinations, baseline segment, integrator, movement argument write and mismatch retirement. No damage authority, broader ammunition, VATS expansion or anatomy work.

New read-only captures, with the user's game left at the main menu, are in runtime-20260915-141533 and runtime-20260915-141903. Their manifests record exact process identity, fixed addresses, hashes and stability across two reads. No injection, calls into the engine, process writes or gameplay. Captured game code is not distributed. The controller helper capture alone does not prove that its branch executes for these projectiles.

Changes:

- Immediately read back the existing checked 12-byte caller input after WriteVector and require byte equality. PHYSICS_ARGUMENT records the actual submitted floats. A failed readback stops the track; an already attempted write cannot be undone.
- Add a read-only CALL observer at 00930475 -> 00575830, the generic movement path's existing position commit, and at 0092F5DC -> 00440460, its alternate node commit. Original call bytes are E8 B6 53 C4 FF and E8 7F 0E B1 FF. Both instructions are present in the captured full movement body, already covered by its fingerprint.
- Each boundary requires the generic move's saved argument-frame return 009BF416, frame-local owner matching a tracked identity, its original input pointer equalling the pending bullet frame +0C, and the same thread. Reads only the argument, generic candidate position at EBP-30, controller presence at EBP-20 and current reference position. No pointer to the optional controller is dereferenced. PHYSICS_BOUNDARY reports the route and vectors. Unsupported reads stop further edits; observations never replace position/node arguments.
- PHYSICS_ARGUMENT_RETURN records the caller's still-live input vector before original distance accounting. Existing actual-displacement comparison and tolerance are unchanged.
- New bridges save GP registers, flags and x87/SSE/MXCSR; restore them and tail-jump once to the original function with the original receiver, arguments and return address. No lock spans an engine call. No new calls to setters, no object-position/angle/velocity writes, no extra engine invocation.
- Four owned CALL sites occupy three pages. Protection is acquired once per distinct span/page, all original bytes verified before patching, and all prior protections restored. Failure attempts rollback of owned bytes and disables activation. Fingerprint normalization accepts only the exact installed owned patches. Existing timing normalization automatically includes these two additional owned sites.

128 tracks, first eight lifetimes with bounded per-step details. New diagnostic rows share existing 7,168 detail/1,024 priority row budgets. Physics does not stop at log quotas. New boundary and argument counters are diagnostics, not proofs of a successful flight calculation. Existing CurrentHit, NativeObserver, DamageEvents, FlightPreview, FlightTiming and NativeLog remain byte-identical to 307. Reload clears transient states. No persisted in-flight trajectory restoration.

Install only DLL/PDB; existing config, NVO.esm, pilot ESP, dependencies and activation retained. Assistant builds and inspects source/assembly/PE/PDB, with no native execution or gameplay. User checkpoint: one private rifle and one private pistol shot at the same distant scenery outside VATS, wait between shots, exit. No extra reload is needed: this packet first needs movement boundary evidence. Read and archive the game-root log directly. Expect the existing displacement mismatch may recur; return the log without adjusting settings. Require write-readback rows and inspect which boundary reports before selecting the next correction.

The separate quiet-bootstrap copy page is prepared only for an existing user-managed NVOCombatBootstrapScript. The installed Data/NVO.esm inspected during this review lacks that record. Do not automatically create a duplicate initialization quest or claim the console issue fixed without the actual repeating text/working script. Not required for the native two-shot check.
